study/*
target
**/dump/**
.DS_Store
rustc-ice-*.txt
# Python bytecode from scripts/
__pycache__/
dist/

# Agent/session scratch, not project source
.claude/
ash_fileio_tmp/

# Staged beside the example by a run, not source: the fixture copies ash's
# runtime here under HashLink's name so the .hdll files bind to it.
examples/heaps_base2d/bin/libhl*.dylib
examples/heaps_base2d/bin/libhl*.so
examples/heaps_base2d/bin/libhl*.dll

# Build output and the .hdll files copied from the base2d example beside it.
examples/heaps_base3d/bin/

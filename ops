{"author":{"id":"609bf866d4a5d716c19faf5d617b3bf3de478230d29602d72aad5aa90a80466f"},"ops":[{"type":3,"timestamp":1789166580,"nonce":"io+2diBMOTCsHlG2ADsDN9C7RgE=","message":"Landed in 8becd42, as proposed here: the build job builds the base (the commit the previous sweep measured, or the workflow_dispatch input 'base') in a worktree sharing target/, ships it as dist/ash-base with its commit, and every leg runs ash_bench.py --ash-base so each timed run of HEAD is followed by one of the base on the same runner. The row carries base.{median_ms,modes,delta_pct}; the console prints 'vs base ±N%' and an A/B section; the site shows the delta under the ash number and names the base commit in the provenance line; the delta is on like modes (7bcd524).\n\nChecked locally with an A/A (a copy of the same binary): the mechanism, the JSON, the merge and the page render. Not yet run through CI: the base build shares target/ with HEAD's and runs first, then deletes the uplifted binaries so HEAD's build must write its own; packaging refuses to ship an ash identical to ash-base. The first sweep after this is pushed verifies it; leaving this open until that sweep has published a delta.","files":null}]}